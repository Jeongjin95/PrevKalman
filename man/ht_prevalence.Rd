% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{ht_prevalence}
\alias{ht_prevalence}
\title{Horvitz-Thompson Prevalence with Block Jackknife Confidence Intervals}
\usage{
ht_prevalence(
  D = NULL,
  Y = NULL,
  R = NULL,
  Z_next = NULL,
  C_t = NULL,
  Sympt_t = NULL,
  gamma = NULL,
  spec,
  sens,
  prepared_data = NULL,
  n_blocks = 20,
  conf_level = 0.95,
  seed = 12345,
  n_cores = safe_mc_cores(),
  days = NULL
)
}
\arguments{
\item{D}{Binary testing indicator matrix.}

\item{Y}{Binary test-result matrix.}

\item{R}{Logical removed-state matrix.}

\item{Z_next}{Matrix of next testing times.}

\item{C_t}{Matrix of most recent clearance times.}

\item{Sympt_t}{Matrix of most recent symptom times.}

\item{gamma}{Clearance support values, typically \code{unique(as.vector(C_t))}.}

\item{spec}{Test specificity.}

\item{sens}{Test sensitivity.}

\item{prepared_data}{Optional output from \code{\link[=prepare_surveillance_data]{prepare_surveillance_data()}}.}

\item{n_blocks}{Number of delete-a-group jackknife blocks. Defaults to \code{20}.}

\item{conf_level}{Confidence level for the interval.}

\item{seed}{Random seed used for jackknife block assignment.}

\item{n_cores}{Number of CPU cores used by \code{\link[=block_jackknife]{block_jackknife()}}.}

\item{days}{Optional day labels. Defaults to \code{prepared_data$days} or
\code{seq_len(ncol(D))}.}
}
\value{
A list with \code{results}, \code{estimate}, \code{se}, \code{lower}, \code{upper}, and the
full \code{jackknife} output.
}
\description{
Computes the daily Horvitz-Thompson prevalence estimate and, by default,
20-block jackknife standard errors and confidence intervals.
}
\examples{
sim <- generate_surveillance_data(
  n_clusters = 4,
  n_per_cluster = 2,
  max_days = 6,
  initial_prev = 0.05,
  test_control = list(
    test.prob = 1 / 6,
    max.gap = 5,
    period.length = 7,
    min.gap = 3,
    symptomatic.prob = 0.25,
    symptom.false.prob = 0.01
  ),
  expose_control = list(
    hazard.scale = 0.1,
    resistance.scale = 0.5,
    interval.length = 6,
    max.hazard = 0.1,
    min.hazard = 0.02
  ),
  recovery_allowed = FALSE,
  recovery_days = 30,
  clearance_days = 3,
  sens = 0.832,
  spec = 0.992,
  seed = 1
)
Z_next <- matrix(NA_real_, nrow = nrow(sim$D), ncol = ncol(sim$D))
Z_next[, ncol(sim$D)] <- ifelse(sim$D[, ncol(sim$D)] == 1, ncol(sim$D), Inf)
for (t in seq.int(ncol(sim$D) - 1, 1)) {
  Z_next[, t] <- ifelse(sim$D[, t] == 1, t, Z_next[, t + 1])
}
Sympt_t <- matrix(0, nrow = nrow(sim$D), ncol = ncol(sim$D))
for (i in seq_len(nrow(sim$D))) {
  last_time <- 0
  for (t in seq_len(ncol(sim$D))) {
    if (sim$symptomatic.mat[i, t] | sim$tracing.mat[i, t]) {
      last_time <- t
    }
    Sympt_t[i, t] <- last_time
  }
}
ht_fit <- ht_prevalence(
  D = sim$D,
  Y = sim$Y,
  R = sim$R,
  Z_next = Z_next,
  C_t = sim$C.t,
  Sympt_t = Sympt_t,
  gamma = unique(as.vector(sim$C.t)),
  spec = 0.992,
  sens = 0.832,
  n_blocks = 4,
  n_cores = 1
)
head(ht_fit$results)
}
