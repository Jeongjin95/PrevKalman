% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{estimate_prevalence_pipeline}
\alias{estimate_prevalence_pipeline}
\title{Estimate the Full HT + Kalman Prevalence Pipeline}
\usage{
estimate_prevalence_pipeline(
  prepared_data,
  spec,
  sens,
  n_blocks = 20,
  conf_level = 0.95,
  seed = 12345,
  ci_method = c("model_based", "ht_se", "ht_se_adjusted", "all"),
  low_test_days = prepared_data$low_test_days,
  n_cores = safe_mc_cores()
)
}
\arguments{
\item{prepared_data}{Output from \code{prepare_surveillance_data}.}

\item{spec}{Test specificity.}

\item{sens}{Test sensitivity.}

\item{n_blocks}{Number of jackknife blocks.}

\item{conf_level}{Confidence level for the jackknife intervals.}

\item{seed}{Random seed for block assignment.}

\item{ci_method}{Confidence-interval option passed to
\code{\link[=fit_kalman_model]{fit_kalman_model()}}.}

\item{low_test_days}{Optional vector of days to mask as missing. Defaults to
\code{prepared_data$low_test_days}.}

\item{n_cores}{Number of cores used by \code{block_jackknife}.}
}
\value{
A list containing the HT estimates and the fitted joint Kalman
results.
}
\description{
Runs the manuscript's main repeated-testing workflow from prepared matrices:
Horvitz-Thompson estimation, block jackknife uncertainty quantification,
missing-day masking, and Kalman model fitting.
}
\examples{
\dontrun{
prep <- prepare_surveillance_data(
  osu_surveillance,
  low_test_threshold = 100,
  symptom_unobserved_days = 1:12
)

fit <- estimate_prevalence_pipeline(
  prep,
  spec = 1,
  sens = 0.832,
  n_blocks = 5,
  n_cores = 1,
  ci_method = "all"
)
}
}
