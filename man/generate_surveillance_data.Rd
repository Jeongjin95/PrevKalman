% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{generate_surveillance_data}
\alias{generate_surveillance_data}
\title{Generate Repeated-Testing Surveillance Data}
\usage{
generate_surveillance_data(
  n_clusters,
  n_per_cluster,
  max_days,
  initial_prev,
  test_fun = NULL,
  sched_fun = sched_fun_opp,
  test_control = list(),
  expose_fun = expose_fun_hazard_quadratic,
  expose_control = list(),
  recovery_allowed,
  recovery_days,
  clearance_days,
  sens,
  spec,
  time_varying_sens = FALSE,
  seed
)
}
\arguments{
\item{n_clusters}{Number of clusters.}

\item{n_per_cluster}{Number of individuals per cluster.}

\item{max_days}{Number of study days.}

\item{initial_prev}{Initial prevalence.}

\item{test_fun}{Testing mechanism. Defaults to \code{test_fun}.}

\item{sched_fun}{Scheduled-testing component. Defaults to \code{sched_fun_opp}.}

\item{test_control}{A list of testing parameters.}

\item{expose_fun}{Exposure mechanism. Defaults to
\code{expose_fun_hazard_quadratic}.}

\item{expose_control}{A list of exposure parameters.}

\item{recovery_allowed}{Logical indicating whether recovery is allowed.}

\item{recovery_days}{Recovery delay in days.}

\item{clearance_days}{Isolation clearance length in days.}

\item{sens}{Test sensitivity.}

\item{spec}{Test specificity.}

\item{time_varying_sens}{Logical; if \code{TRUE}, sensitivity varies with time
since infection.}

\item{seed}{Random seed.}
}
\value{
A list of simulation matrices and metadata.
}
\description{
Simulates repeated testing, isolation, and infection dynamics under the
schedule used in the manuscript.
}
\examples{
sim <- generate_surveillance_data(
  n_clusters = 5,
  n_per_cluster = 2,
  max_days = 7,
  initial_prev = 0.05,
  test_control = list(
    test.prob = 1 / 6,
    max.gap = 5,
    period.length = 7,
    min.gap = 3,
    symptomatic.prob = 0.25,
    symptom.false.prob = 0.01
  ),
  expose_control = list(
    hazard.scale = 0.1,
    resistance.scale = 0.5,
    interval.length = 7,
    max.hazard = 0.1,
    min.hazard = 0.02
  ),
  recovery_allowed = FALSE,
  recovery_days = 30,
  clearance_days = 5,
  sens = 0.832,
  spec = 0.992,
  seed = 1
)
names(sim)
}
