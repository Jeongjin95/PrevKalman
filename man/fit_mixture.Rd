% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{fit_mixture}
\alias{fit_mixture}
\title{Fit the CI-6 Convex-Combination Mixture}
\usage{
fit_mixture(
  x,
  state = c("filter", "smoother", "both"),
  conf_level = 0.95,
  init_level = NULL,
  init_slope = 0,
  q_level_start = 1e-06,
  q_slope_start = 1e-05,
  q_lower = 1e-10,
  q_upper = 0.01,
  days = NULL
)
}
\arguments{
\item{x}{Output from \code{\link[=ht_prevalence]{ht_prevalence()}}. This function requires the
leave-one-block-out jackknife estimates stored in \code{x$jackknife}.}

\item{state}{One of \code{"filter"}, \code{"smoother"}, or \code{"both"}.}

\item{conf_level}{Confidence level for the interval.}

\item{init_level}{Optional initial level. Defaults to the first observed
estimate after masking missing days.}

\item{init_slope}{Optional initial slope.}

\item{q_level_start}{Starting value for the level variance.}

\item{q_slope_start}{Starting value for the slope variance.}

\item{q_lower}{Lower bound used during optimization.}

\item{q_upper}{Upper bound used during optimization.}

\item{days}{Optional day labels.}
}
\value{
A list with \code{results}, one row per day and requested state, and
\code{joint_fit}, the fitted joint Kalman model. The \code{results} data frame
includes the CI-6 mixture estimate, weight, correlation estimate, and
combination standard error.
}
\description{
Combines the Horvitz-Thompson estimator with the joint Kalman filter or
smoother using the CI-6 convex-combination construction.
}
\examples{
\dontrun{
# D: testing indicator matrix
# Y: test-result matrix
# R: removed/exempt-state matrix
# Z.next: next testing-time matrix
# C.t: most recent clearance-time matrix
# Sympt.t: most recent symptom/contact-time matrix
# gamma: support values of C.t

ht_fit <- ht_prevalence(
  D = D,
  Y = Y,
  R = R,
  Z.next = Z.next,
  C.t = C.t,
  Sympt.t = Sympt.t,
  gamma = gamma,
  SPEC = 1,
  SENS = 0.832,
  max.days = ncol(D)
)

fit_mixture(
  x = ht_fit,
  state = "smoother"
)
}
}
