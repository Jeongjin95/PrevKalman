% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimation.R
\name{fit_kalman_model}
\alias{fit_kalman_model}
\title{Fit the Joint Kalman Prevalence Model}
\usage{
fit_kalman_model(
  y = NULL,
  r_t = NULL,
  x = NULL,
  state = c("filter", "smoother", "both"),
  ci_method = c("model_based", "ht_se", "ht_se_adjusted", "all"),
  conf_level = 0.95,
  low_test_days = NULL,
  init_level = NULL,
  init_slope = 0,
  q_level_start = 1e-06,
  q_slope_start = 1e-05,
  q_lower = 1e-10,
  q_upper = 0.01,
  days = NULL
)
}
\arguments{
\item{y}{Optional numeric vector of prevalence estimates.}

\item{r_t}{Optional numeric vector of observation variances.}

\item{x}{Optional output from \code{\link[=ht_prevalence]{ht_prevalence()}} or a data frame containing
\code{estimate} and \code{se} columns.}

\item{state}{One of \code{"filter"}, \code{"smoother"}, or \code{"both"}.}

\item{ci_method}{Confidence-interval option. Choose from
\code{"model_based"} (default), \code{"ht_se"}, \code{"ht_se_adjusted"}, or \code{"all"}.}

\item{conf_level}{Confidence level for the interval.}

\item{low_test_days}{Optional integer vector of days to treat as missing
before fitting the state-space model.}

\item{init_level}{Optional initial level. Defaults to the first observed
estimate after masking missing days.}

\item{init_slope}{Optional initial slope.}

\item{q_level_start}{Starting value for the level variance.}

\item{q_slope_start}{Starting value for the slope variance.}

\item{q_lower}{Lower bound used during optimization.}

\item{q_upper}{Upper bound used during optimization.}

\item{days}{Optional day labels.}
}
\value{
A list with \code{results}, one row per day and CI option, and
\code{joint_fit}, the full fitted joint model object.
}
\description{
Fits the joint local linear trend Kalman model and returns daily estimates
with one or more confidence-interval options for either the filter or the
smoother.
}
\examples{
y <- c(0.01, 0.02, NA, 0.03, 0.025)
r_t <- c(0.0001, 0.0001, NA, 0.0002, 0.00015)
fit_kalman_model(
  y = y,
  r_t = r_t,
  state = "filter",
  ci_method = "all"
)
}
